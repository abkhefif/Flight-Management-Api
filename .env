# URL de connexion à la base de données
DATABASE_URL=sqlite:///./fms.db
# → sqlite:/// = type de DB (SQLite)
# → ./fms.db = fichier dans le dossier courant

# Nom du projet (pour la doc Swagger)
PROJECT_NAME=Flight Management System

# Préfixe de toutes les routes API
API_V1_PREFIX=/api/v1
# → Toutes les routes seront /api/v1/airports, /api/v1/flights, etc.

# Environnement (dev, staging, prod)
ENVIRONMENT=development
# → Change le comportement (logs verbeux en dev, etc.)
